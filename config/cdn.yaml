kind: 'CDN'
version: '1'
data:
  originSelectors:
    rules:
      - name: route-to-personalization
        when: { reqProperty: path, equals: "/personalization-context" }
        action:
          type: selectAemOrigin
          originName: edgefunction-personalization
          skipCache: true
